
<div class="col-xs-3">
  <%= render 'users/info', user: @user %>
  <%= render 'books/form', book: Book.new %>
</div>

<div class="col-xs-9">
  <% @books.each do |book| %>

    <%= link_to book.title, book_path(book.id) %>
    <%= book.body %>

  <% end %>
</div>

