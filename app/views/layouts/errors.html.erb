<% if obj.errors.any? %>
  <div class="error_explantion">
    <h3><%= pluralize(obj.errors.count, "error") %>errors prohibited this obj from being saved:</h3>
    <ul>
      <%= obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>