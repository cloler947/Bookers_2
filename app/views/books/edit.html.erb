
<h2>Editing Book</h2>
<%= form_for(@book) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label "Opinion" %>
  <%= f.text_area :body %>

  <%= f.submit 'Update Book' %>

<% end %>